# 董事长微博数据采集工作流程使用指南

## 概述

本工作流程基于现有的微博搜索爬虫程序，实现了从Excel文件读取董事长任职信息，自动爬取相关微博数据，并将结果整合输出的完整流程。

## 功能特性

- 自动读取包含董事长任职信息的Excel文件
- 根据任期自动展开为公司-年度组合（2019-2023年）
- 为每个组合构造搜索关键词："公司名称 + 董事长姓名"
- 调用现有Scrapy爬虫获取指定年份的微博数据
- 每个公司-年度组合最多保留5条微博（可配置）
- 将微博数据与原始Excel数据合并
- 支持输出为Excel或CSV格式

## 环境准备

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

需要的主要依赖包括：
- scrapy（微博爬虫框架）
- pandas（数据处理）
- openpyxl（Excel文件处理）
- Pillow（图片处理，爬虫需要）

### 2. 配置Cookie

在运行工作流之前，必须先配置微博的Cookie。请按照以下步骤操作：

1. 用Chrome浏览器打开 https://weibo.com/
2. 登录微博账号
3. 按F12打开开发者工具
4. 在Network标签页中找到任意请求的Headers
5. 复制Cookie值
6. 在 `weibo/settings.py` 文件中，将Cookie值替换到 `DEFAULT_REQUEST_HEADERS` 的 `cookie` 字段

详细的Cookie获取方法请参考主README.md文件。

## 输入文件格式

输入Excel文件应包含以下列（列名必须匹配）：

| 列名 | 说明 | 示例 |
|------|------|------|
| 个人ID | 董事长个人唯一标识 | 30587115 |
| 股票代码 | 公司股票代码 | 688001 |
| 公司名称 | 公司名称 | 华兴源创 |
| 姓名 | 董事长姓名 | 陈文源 |
| 职务 | 职务名称 | 董事长 |
| 性别 | 性别 | 男 |
| 出生日期 | 出生日期 | 196811 |
| 学历 | 学历 | |
| 国籍 | 国籍 | 中国 |
| 起始年份 | 任期起始年份 | 2018 |
| 终止年份 | 任期终止年份 | 2024 |
| 任期区间 | 任期区间描述 | 2018-2024 |

### 示例数据

```
个人ID    股票代码  公司名称  姓名    职务    性别  出生日期  学历  国籍  起始年份  终止年份  任期区间
30587115  688001   华兴源创  陈文源  董事长  男    196811         中国  2018      2024      2018-2024
```

### 创建示例输入文件

运行以下命令可以创建一个示例输入文件：

```bash
python create_example_input.py
```

这将生成 `example_chairman_data.xlsx` 文件。

## 使用方法

### 基本用法

```bash
python workflow.py --input chairman_data.xlsx --output result.xlsx
```

### 参数说明

- `--input`, `-i`: （必需）输入的Excel文件路径
- `--output`, `-o`: （必需）输出的Excel或CSV文件路径
- `--max-posts`, `-m`: 每个公司-年度组合最多保留的微博数量（默认5条）
- `--years`, `-y`: 目标年份列表（默认2019-2023）

### 使用示例

#### 示例1：基本使用

```bash
python workflow.py --input chairman_data.xlsx --output result.xlsx
```

#### 示例2：自定义微博数量

只保留每个公司-年度的前3条微博：

```bash
python workflow.py --input chairman_data.xlsx --output result.csv --max-posts 3
```

#### 示例3：指定年份范围

只查询2020-2022年的数据：

```bash
python workflow.py --input chairman_data.xlsx --output result.xlsx --years 2020 2021 2022
```

## 输出格式

输出文件包含以下内容：

1. **原始Excel的所有列**：保留输入文件的所有原始字段
2. **查询年份**：当前记录对应的查询年份
3. **关键词**：使用的搜索关键词（公司名称 + 董事长姓名）
4. **微博数量**：实际获取到的微博数量
5. **微博内容字段**：每条微博的详细信息（最多5条）
   - 微博1_内容、微博1_发布时间、微博1_转发数、微博1_评论数、微博1_点赞数
   - 微博2_内容、微博2_发布时间、微博2_转发数、微博2_评论数、微博2_点赞数
   - ...
   - 微博5_内容、微博5_发布时间、微博5_转发数、微博5_评论数、微博5_点赞数

### 输出示例

每个公司-年度组合对应输出文件中的一行：

| 个人ID | 股票代码 | 公司名称 | 姓名 | ... | 查询年份 | 关键词 | 微博数量 | 微博1_内容 | 微博1_发布时间 | ... |
|--------|----------|----------|------|-----|----------|--------|----------|------------|----------------|-----|
| 30587115 | 688001 | 华兴源创 | 陈文源 | ... | 2019 | 华兴源创 陈文源 | 3 | 微博内容1 | 2019-05-01 | ... |
| 30587115 | 688001 | 华兴源创 | 陈文源 | ... | 2020 | 华兴源创 陈文源 | 5 | 微博内容1 | 2020-03-15 | ... |

## 工作流程说明

1. **读取Excel文件**：从指定路径读取包含董事长信息的Excel文件

2. **数据展开**：
   - 解析每条记录的起始年份和终止年份
   - 计算与目标年份（2019-2023）的交集
   - 为每个在任年份生成一条新记录

3. **构造关键词**：
   - 格式：`公司名称 + 空格 + 董事长姓名`
   - 示例：`华兴源创 陈文源`

4. **调用爬虫**：
   - 为每个关键词-年份组合调用Scrapy爬虫
   - 设置时间范围为该年的1月1日至12月31日
   - 限制结果数量为指定的max_posts值

5. **结果整合**：
   - 将爬取的微博数据与原始记录合并
   - 按照输出格式组织数据

6. **保存结果**：
   - 根据输出文件扩展名（.xlsx或.csv）保存结果
   - 使用UTF-8编码，确保中文正常显示

## 注意事项

1. **Cookie配置**：必须配置有效的微博Cookie，否则爬虫无法正常工作

2. **访问频率**：程序会在每次请求之间添加延迟（默认10秒），避免被反爬虫系统封禁

3. **数据完整性**：
   - 如果某个公司-年度没有找到微博，输出中仍会保留该行，但微博字段为空
   - 如果找到的微博少于5条，只填充实际数量的字段

4. **执行时间**：
   - 工作流程会依次处理每个公司-年度组合
   - 总执行时间取决于记录数量和网络状况
   - 对于大量数据，建议分批处理

5. **结果文件**：
   - 爬虫会在 `结果文件/` 目录下创建临时CSV文件
   - 这些文件可以手动清理，或在下次运行时覆盖

6. **错误处理**：
   - 如果某个关键词爬取失败，程序会继续处理其他记录
   - 错误信息会显示在控制台

7. **中断恢复**：
   - 当前版本不支持断点续传
   - 如果中途中断，需要重新开始

## 常见问题

### Q1: 提示"cookie无效或已过期"

**A**: 需要重新获取微博Cookie并更新到 `weibo/settings.py` 文件中。

### Q2: 某些公司-年度没有获取到微博

**A**: 这是正常现象，可能的原因：
- 该年份确实没有相关微博
- 关键词搜索结果为空
- 微博已被删除

### Q3: 程序执行很慢

**A**: 这是正常的，原因：
- 每次请求之间有延迟（默认10秒）
- 网络请求需要时间
- 可以适当减小 `DOWNLOAD_DELAY` 值，但可能增加被封禁风险

### Q4: 输出文件中文乱码

**A**: 
- Excel文件：应该不会出现乱码，使用的是openpyxl
- CSV文件：使用UTF-8-sig编码，Excel应该能正确打开

### Q5: 如何只查询特定几个年份？

**A**: 使用 `--years` 参数指定：
```bash
python workflow.py --input data.xlsx --output result.xlsx --years 2020 2022
```

## 技术架构

- **Scrapy**：微博爬虫框架，负责网页抓取和数据解析
- **Pandas**：数据处理和分析，负责Excel读写和数据转换
- **Subprocess**：进程管理，负责调用Scrapy爬虫
- **Argparse**：命令行参数解析

## 文件说明

- `workflow.py`：主工作流程脚本
- `create_example_input.py`：创建示例输入文件的脚本
- `WORKFLOW_GUIDE.md`：本使用指南
- `weibo/spiders/search.py`：微博搜索爬虫（原有）
- `weibo/settings.py`：爬虫配置文件（原有）

## 扩展功能

如果需要自定义功能，可以修改 `workflow.py` 中的相关方法：

1. **修改关键词格式**：修改 `expand_to_company_years()` 方法中的 `keyword` 生成逻辑
2. **修改输出格式**：修改 `format_output()` 方法
3. **添加过滤条件**：在 `crawl_weibo()` 方法中添加爬虫参数

## 许可证

本工作流程基于原weibo-search项目开发，遵循原项目的许可证。
